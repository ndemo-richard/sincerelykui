/// <reference types="react" />
interface Props {
    disabled: Boolean;
    className?: string;
    typeName: string;
    selectedIds: Set<string>;
    onDelete: () => void;
}
declare function BulkActionsMenu({ disabled, className, selectedIds, typeName, onDelete, }: Props): JSX.Element;
export default BulkActionsMenu;
